include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # Same leak-prone-resource rules as the root package.
    close_sinks: error
    cancel_subscriptions: error
  exclude:
    - build/**
    - android/**
    - ios/**

linter:
  rules:
    - cancel_subscriptions
    - close_sinks
