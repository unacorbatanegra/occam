name: occam
description: Minimalist state managment
version: 1.0.0
homepage: https://github.com/unacorbatanegra/occam

environment:
  sdk: ">=2.15.0 <3.0.0"
  # flutter: ">=3..0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.7.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
