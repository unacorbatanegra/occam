/// Opt-in lifecycle logging.
///
/// When `true`, every [StateController] prints on init and dispose.
class OccamDebug {
  /// Set to `true` to print a line on every [StateController] init/dispose.
  static bool debug = false;
}
